# Browser-facing backend API base (exposed to the client).
NUXT_PUBLIC_API_BASE=http://localhost:8000/api/v1

# Server-side (SSR) backend API base — internal Docker network hostname.
NUXT_API_BASE_INTERNAL=http://backend:8000/api/v1
